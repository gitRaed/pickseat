"use strict";;let sql;_5cc‍.x([["registerUser",()=>registerUser]]);_5cc‍.w("mssql",[["default",["sql"],function(v){sql=v}]]);



var config = {
    user: 'sa',
    password: 'raed',
    server: 'localhost',
    database: 'pixil',
    "options": {
        "encrypt": true,
        "enableArithAbort": true
        },
};

       async function registerUser(nom, prenom, email, numero, typeUser, motDePasse) {

    // console.log(nom, prenom, email, numero, typeUser, motDePasse);
    try {
        const pool = await new sql.ConnectionPool(config).connect();
        await pool.request()
            .input('nom', nom)
            .input('prenom', prenom)
            .input('email', email)
            .input('numero', numero)
            .input('typeUser', typeUser)
            .input('motDePasse', motDePasse)
            .query('INSERT INTO utilisateur VALUES (@nom, @prenom, @email, @numero, @typeUser, @motDePasse)');
    } catch (error) {
        _5cc‍.g.console.log('Requete-register, erreur : ' + error);
    }

}