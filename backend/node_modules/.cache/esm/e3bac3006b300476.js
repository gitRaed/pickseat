"use strict";;let sql;_6b3‍.x([["registerUser",()=>registerUser],["registerFinal",()=>registerFinal],["resetPasswordDb",()=>resetPasswordDb]]);_6b3‍.w("mssql",[["default",["sql"],function(v){sql=v}]]);



var config = {
    user: 'sa',
    password: 'raed',
    server: 'localhost',
    database: 'pickseat',
    "options": {
        "encrypt": true,
        "enableArithAbort": true
    },
};

       async function registerUser(nom, prenom, email, numero, typeUser, status_validation, status_compte, motDePasse) {

    // console.log(nom, prenom, email, numero, typeUser, motDePasse);
    try {
        const pool = await new sql.ConnectionPool(config).connect();
        await pool.request()
            .input('nom', nom)
            .input('prenom', prenom)
            .input('email', email)
            .input('numero', numero)
            .input('typeUser', typeUser)
            .input('motDePasse', motDePasse)
            .input('status_validation', status_validation)
            .input('status_compte', status_compte)
            .query('INSERT INTO utilisateurs VALUES (@nom, @prenom, @email, @numero, @typeUser, @status_validation, @status_compte, @motDePasse)');
    } catch (error) {
        _6b3‍.g.console.log('Requete-register, erreur : ' + error);
    }

}

       async function registerFinal(email, status_validation, status_compte,) {

    try {
        const pool = await new sql.ConnectionPool(config).connect();
        await pool.request()
            .input('email', email)
            .input('status_validation', status_validation)
            .input('status_compte', status_compte)
            .query('UPDATE utilisateurs SET status_validation = @status_validation, status_compte = @status_compte WHERE email = @email');
    } catch (error) {
        _6b3‍.g.console.log('register final error : ' + error);
    }
}

       async function resetPasswordDb(email, motDePasse) {

    try {
        const pool = await new sql.ConnectionPool(config).connect();
        await pool.request()
            .input('email', email)
            .input('motDePasse', motDePasse)
            .query('UPDATE utilisateurs SET motDePasse = @motDePasse WHERE email = @email');
    } catch (error) {
        _6b3‍.g.console.log('register final error : ' + error);
    }
}