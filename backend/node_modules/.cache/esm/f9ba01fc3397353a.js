"use strict";;let isAuth,authCodeEmail;_339‍.x([["verif",()=>verif]]);_339‍.w("../../serviceLayer/service/isAuth.mjs",[["isAuth",["isAuth"],function(v){isAuth=v}]]);_339‍.w("../../serviceLayer/code_data.mjs",[["authCodeEmail",["authCodeEmail"],function(v){authCodeEmail=v}]]);







       async function verif(token, email, response) {

    try {
        
        const verifToken = await isAuth(token);
        const verifEmail = await authCodeEmail(email);

        const result = {
            bool: false,
            message : ''
        };

        if (verifToken.message !== '') {

            result.message = verifToken.message;
        } 
        
        if (verifEmail.auth === false) {

            result.message += 'L\'utilisateur n\'existe pas';
        } 

        return result.message;

    } catch (error) {
        _339‍.g.console.log('Service verif error : ' + error);
        return error;
    }
}